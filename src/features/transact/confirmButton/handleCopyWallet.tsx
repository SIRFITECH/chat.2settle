//  const handleCopyWallet = (wallet: string, ) => {
//    navigator.clipboard.writeText(wallet).then(() => {
//      setState((prev) => ({
//        ...prev,
//        isCopied: true,
//        hasCopyButtonBeenClicked: true,
//      }));
//      setTimeout(() => setState((prev) => ({ ...prev, isCopied: false })), 3000);
//    });
//  };